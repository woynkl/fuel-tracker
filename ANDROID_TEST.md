# Android 真机验收

本清单写给安装 APK 的普通用户，不需要 Node.js、npm、Android Studio、ADB 或 USB 调试。

准备两个文件：

- `fuel-tracker-0.1.0-v1-initial.apk`：首次安装
- `fuel-tracker-0.1.0-v2-upgrade.apk`：覆盖升级

重要提醒：数据只保存在 APP 中。卸载 APP 或清除 APP 数据会删除本地记录。请定期把
JSON 备份保存到 APP 外部；覆盖升级前也先导出一份保险备份。

## A. 安装与启动

1. 在手机文件管理器中打开 `v1-initial.apk`，按系统提示允许本次来源安装应用。
2. 安装并打开“油耗记录”。
3. 确认没有登录、服务器或网络配置页面，首页为空状态且可操作。
4. 确认页面没有横向滚动，中文、按钮和表单没有被截断。

## B. 首条基准记录

新增一条“加满”记录：里程 `10000 km`、金额 `380 CNY`、油价 `7.60 CNY/L`，
日期使用当天。保存后应显示 `50 L`，油耗仍为 `--`（第一条只作为基准）。关闭并重新
打开 APP，记录仍应存在。

## C. Full-to-full

再新增一条“加满”记录：里程 `10550 km`、金额 `380 CNY`、油价 `7.60 CNY/L`。
应得到 `550 km`，油耗约 `9.09 L/100km`。

## D. Partial fill

另建一个完整测试周期：加满 → 未加满 → 加满。确认中间一次的升数和金额累计进最终
周期，未加满记录本身不会提前产生完整周期油耗。

## E. 删除与重算

删除一条测试记录。确认历史记录、最近周期、平均油耗、累计里程和累计金额均重新计算，
页面没有残留删除前的统计。

## F. 完全离线

开启飞行模式并关闭 Wi-Fi，完全退出后重新打开 APP。在离线状态下依次查看历史、新增、
删除、查看统计并导出 JSON，均应正常工作且不要求联网。

## G. JSON 备份与恢复

1. 打开“数据管理”，点击“导出数据”。
2. 在 Android 系统面板中选择“保存到文件”或其它 APP 外部位置；不要只把文件留在 APP
   临时目录。
3. 确认获得 `fuel-backup-YYYY-MM-DD.json`。
4. 准备好当前数据的保险备份后，使用测试数据进行恢复验证。
5. 点击“选择备份文件”，选择刚才的 JSON；确认覆盖后检查历史、里程、金额和油耗。
6. 如有旧 server 版本导出的 schemaVersion 1 JSON，也应能用同一入口导入。

## H. 强制关闭与重启手机

确保已有至少两条记录。强制关闭 APP 再打开，随后重启手机再打开；两次都应保留数据。

## I. 覆盖升级（不要卸载）

1. 先从 v1 APP 导出一份保险 JSON 备份。
2. **不要卸载 v1，不要清除其数据。**
3. 直接打开 `fuel-tracker-0.1.0-v2-upgrade.apk` 并安装，系统应将其识别为更新。
4. 打开升级后的 APP，确认 v1 中的全部记录仍存在，并可继续新增记录。

如果系统要求先卸载旧版，或提示签名/package 冲突，请停止测试并反馈；不要通过卸载来
绕过，否则无法验证升级数据保留。

## J. 反馈模板

```text
APK 版本/Commit：
手机型号：
Android 版本：
安装方式：全新安装 / 覆盖安装

A 安装启动：通过 / 失败
B 首条基准：通过 / 失败
C Full-to-full：通过 / 失败
D Partial fill：通过 / 失败
E 删除重算：通过 / 失败
F 完全离线：通过 / 失败
G 备份恢复：通过 / 失败
H 重启持久化：通过 / 失败
I 覆盖升级：通过 / 失败

问题描述：
复现步骤：
期望结果：
实际结果：
截图：如有
```
