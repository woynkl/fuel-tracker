# Public hostname only, without scheme or path (for example: fuel.example.com)
APP_DOMAIN=
ACME_EMAIL=

# Run `npm run auth:generate` and paste the generated values below unchanged.
APP_PASSWORD_HASH=
SESSION_SECRET=

DATABASE_URL=file:/app/prisma/db/dev.db
